{
  "private": true,
  "homepage": "https://github.com/tkmpraveens/nextjs-learning/routing-concepts/",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "export": "next export",
    "deploy": "npm run build && npm run export && touch ./out/.nojekyll && git add . && git commit -m 'Deploying' && git push -f origin deploy"
  },
  "dependencies": {
    "next": "latest",
    "react": "18.2.0",
    "react-dom": "18.2.0"
  },
  "devDependencies": {
    "gh-pages": "^5.0.0"
  }
}
